import React from "react";
import { ContactsContainer } from "./Contacts.styled";

export const Contacts = () => {
    return (
        <ContactsContainer>
            <p>Контакты</p>
        </ContactsContainer>
    )
}